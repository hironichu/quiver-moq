# Architecture

Core component layout for `MOQCore`.

## Overview

`MOQCore` separates transport capabilities (`MOQTransport`) from session coordination (`MOQSession`) and data modeling (`MOQTrack`, `MOQObject`).

## See Also

- <doc:MOQCore>
- <doc:WithQUICAndHTTP3>
