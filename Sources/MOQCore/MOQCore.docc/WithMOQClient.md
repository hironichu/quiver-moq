# WithMOQClient

Using `MOQCore` from client workflows.

## Overview

Clients use `MOQSession` to subscribe to remote tracks and consume `MOQObject` data in app-specific rendering or processing pipelines.

## See Also

- <doc:MOQCore>
