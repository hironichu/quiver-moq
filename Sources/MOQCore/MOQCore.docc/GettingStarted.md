# GettingStarted

Quick start for modeling Media over QUIC sessions with `MOQCore`.

## Overview

Create a `MOQSession`, publish tracks with `MOQTrack`, and exchange `MOQObject` payloads over a transport that conforms to `MOQTransport`.

## See Also

- <doc:MOQCore>
- <doc:SessionLifecycle>
